# @package _global_
# Pretraining mode - train models from scratch

defaults:
  - _defaults

config:
  do_train: true
  do_eval: false
  supervised_lm_ids: all
  eval_env_interface_args: null
  eval_env_interface_class: null
