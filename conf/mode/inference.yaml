# @package _global_
# Inference mode - evaluate pretrained models

defaults:
  - _defaults

config:
  do_train: false
  do_eval: true
  python_log_level: DEBUG
  train_env_interface_args: null
  train_env_interface_class: null
