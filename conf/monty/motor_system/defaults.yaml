# @package config.monty_config.motor_system_config
defaults:
  - policy: base
  - action_space: distant_agent

motor_system_class: ${monty.class:tbp.monty.frameworks.models.motor_system.MotorSystem}
motor_system_args:
  policy_class: ${monty.class:tbp.monty.frameworks.models.motor_policies.BasePolicy}
  policy_args:
    action_sampler_class: ${monty.class:tbp.monty.frameworks.actions.action_samplers.UniformlyDistributedSampler}
