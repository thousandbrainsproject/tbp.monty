# @package config.monty_config.motor_system_config
# Informed motor policy with 1 degree rotation steps
defaults:
  - informed_no_trans

motor_system_args:
  policy_args:
    action_sampler_args:
      rotation_degrees: 1.0
