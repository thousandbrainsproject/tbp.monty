# @package _global_
# Test: Evaluation with more epochs than rotations
#
# Tests behavior when n_eval_epochs > len(rotations)

defaults:
  - /experiment/tests_v2/eval@_here_

config:
  n_eval_epochs: 4 # > len(eval_rotations)
  logging:
    run_name: test_eval_gt
