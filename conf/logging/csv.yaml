# @package config.logging
# Minimal logging configuration - CSV stats only
defaults:
  - defaults

monty_handlers:
  - ${monty.class:tbp.monty.frameworks.loggers.monty_handlers.BasicCSVStatsHandler}
wandb_handlers: []
