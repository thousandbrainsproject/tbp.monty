# @package _global_

defaults:
  - /monty: naive_scan_5_displacement1_camera_dist1
  - /environment: habitat_ycb_dist_agent_semantics0
  - /env_interface: train_distinctobj_predefined_rotation
  - /logging: silent_info_train
  - config: train_surf_epochall

experiment:
  _target_: tbp.monty.frameworks.experiments.pretraining_experiments.MontySupervisedObjectPretrainingExperiment
  config:
    logging:
      run_name: supervised_pre_training_base
