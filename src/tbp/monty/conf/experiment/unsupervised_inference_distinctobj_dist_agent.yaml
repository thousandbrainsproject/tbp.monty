# @package _global_

defaults:
  - /monty: informed_noresetevidence1_camera_dist_noise1
  - /env_interface: eval_distinctobj_random_rotation
  - /logging: basic
  - /environment: habitat_dist_agent_semantics0
  - config: eval_dist_maxeval100_epoch10_distinctobj

experiment:
  _target_: tbp.monty.frameworks.experiments.object_recognition_experiments.MontyObjectRecognitionExperiment
  config:
    logging:
      run_name: unsupervised_inference_distinctobj_dist_agent
