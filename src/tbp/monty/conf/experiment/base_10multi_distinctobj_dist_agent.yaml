# @package _global_

defaults:
  - /monty: informed_10_evidence1_camera1
  - /env_interface: eval_distractors_predefined_rotation
  - /logging: basic
  - /environment: habitat_dist_agent_semantics1
  - config: eval_dist_epochall

experiment:
  _target_: tbp.monty.frameworks.experiments.object_recognition_experiments.MontyObjectRecognitionExperiment
  config:
    logging:
      run_name: base_10multi_distinctobj_dist_agent
