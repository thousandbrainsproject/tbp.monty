# @package _global_

defaults:
  - /monty: naive_scan_5_displacement5_camera5
  - /environment: habitat_ycb_dist_agent_sensors5_semantics0
  - /env_interface: train_allobj_predefined_rotation
  - /logging: silent_info_train
  - config: train_surf_epochall

experiment:
  _target_: tbp.monty.frameworks.experiments.pretraining_experiments.MontySupervisedObjectPretrainingExperiment
  config:
    logging:
      run_name: supervised_pre_training_5lms_all_objects
