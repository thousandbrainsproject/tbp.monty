# @package _global_

defaults:
  - /monty: naive_scan_evidence2_camera_dist2
  - /environment: habitat_dist_agent_sensors2
  - /env_interface: train_flatobjs_predefined_rotation
  - /logging: silent_info_train
  - config: train_dist

experiment:
  _target_: tbp.monty.frameworks.experiments.pretraining_experiments.MontySupervisedObjectPretrainingExperiment
  config:
    logging:
      run_name: supervised_pre_training_flat_objects_wo_logos
