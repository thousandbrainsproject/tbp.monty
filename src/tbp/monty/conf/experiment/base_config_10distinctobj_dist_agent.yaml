# @package _global_

defaults:
  - /monty: informed_evidence1_camera1
  - /environment: habitat_dist_agent
  - /env_interface: eval_predefined
  - /logging: basic
  - config: eval_dist_epochall

experiment:
  _target_: tbp.monty.frameworks.experiments.object_recognition_experiments.MontyObjectRecognitionExperiment
  config:
    logging:
      run_name: base_config_10distinctobj_dist_agent
