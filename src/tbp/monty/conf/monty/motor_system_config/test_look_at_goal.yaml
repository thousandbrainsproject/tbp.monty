# @package experiment.config.monty_config.motor_system_config

defaults:
  - policy: look_at_goal
  - policy_selector: single_policy_selector

_target_: tbp.monty.frameworks.models.motor_system.MotorSystem
