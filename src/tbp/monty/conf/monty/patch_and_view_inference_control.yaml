# @package config.monty_config
# Patch and view finder agent for control experiments.
# Uses hsv-only tolerances (no principal_curvatures_log).

defaults:
  - monty_class: graph_matching
  - monty_args: defaults
  - motor_system: naive_scan_spiral
  - learning_module/evidence_distant_hsv_only@learning_module_configs.learning_module_0
  - sensor_module: sm0_habitatsm_sm1_viewfinder
  - connectivity: patch_view_single_lm
