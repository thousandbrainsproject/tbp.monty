# @package experiment.config.monty_config

defaults:
  - motor_system_config: informed_2
  - learning_module: evidence_2lm_gsg_mmd0001
  - sensor_module: 2sm_camera_dist
  - connectivity: 2lm_2sm

monty_class: ${monty.class:tbp.monty.frameworks.models.evidence_matching.model.MontyForEvidenceGraphMatching}
monty_args:
  num_exploratory_steps: 0
  min_eval_steps: 3
  min_train_steps: 500
  max_total_steps: 2500
