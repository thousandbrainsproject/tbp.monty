# @package experiment.config.monty_config.motor_system

defaults:
  - action_sampler: constant

policy:
  _target_: tbp.monty.frameworks.models.motor_policies.InformedPolicy
  use_goal_state_driven_actions: true
  agent_id: ${monty.agent_id:agent_id_0}
